name: CI Pipeline

on:
  push:
    branches:
      - main
      - 'feature/**'
      - 'bugfix/**'
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read

jobs:
  lint:
    name: 🔍 Lint All Services
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff
      - name: Run linter on backend
        run: ruff check backend/
      - name: Run linter on auth-service
        run: ruff check services/auth-service/ --ignore E999 || echo "Auth service directory not found or empty"
      - name: Run linter on task-service
        run: ruff check services/task-service/ --ignore E999 || echo "Task service directory not found or empty"

  test-services:
    name: 🧪 Test ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: [lint]
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: backend
            path: ./backend
            runtime: python
          - service: auth-service
            path: ./services/auth-service
            runtime: python
          - service: task-service
            path: ./services/task-service
            runtime: python
          - service: frontend
            path: ./frontend
            runtime: node
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Python Logic
      - name: Setup Python
        if: matrix.runtime == 'python'
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: '${{ matrix.path }}/requirements.txt'
        continue-on-error: true

      - name: Install Backend Deps
        if: matrix.runtime == 'python'
        working-directory: ${{ matrix.path }}
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov httpx
        continue-on-error: true

      - name: Run Python Tests
        if: matrix.runtime == 'python'
        working-directory: ${{ matrix.path }}
        env:
          DATABASE_URL: sqlite:///./test.db
          SECRET_KEY: "kelarin_secret_key_untuk_testing_doang"
        run: pytest --cov=. --cov-report=term-missing || echo "No tests found in ${{ matrix.service }}"
        continue-on-error: true

      # Node.js Logic
      - name: Setup Node.js
        if: matrix.runtime == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: '${{ matrix.path }}/package-lock.json'

      - name: Install Frontend Deps
        if: matrix.runtime == 'node'
        working-directory: ${{ matrix.path }}
        run: npm ci

      - name: Build Frontend
        if: matrix.runtime == 'node'
        working-directory: ${{ matrix.path }}
        run: npm run build --if-present

      - name: Run Frontend Tests
        if: matrix.runtime == 'node'
        working-directory: ${{ matrix.path }}
        run: npm test -- --passWithNoTests
